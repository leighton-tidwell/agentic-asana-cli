name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    name: Node ${{ matrix.node }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [20, 22]
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run format:check
      - run: npm test
      - run: npm run build
      - run: npm run test:api-coverage
      - run: npm run test:packaging
      - name: Probe packed CLI entrypoint
        run: |
          mkdir -p "$RUNNER_TEMP/asn-pack"
          npm pack --pack-destination "$RUNNER_TEMP/asn-pack"
          npm install --prefix "$RUNNER_TEMP/asn-install" --global "$RUNNER_TEMP/asn-pack/"*.tgz
          test "$("$RUNNER_TEMP/asn-install/bin/asn" --version)" = "$(node -p 'require(\"./package.json\").version')"

  secret-scan:
    name: Gitleaks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@dcedce43c6f43de0b836d1fe38946645c9c638dc # v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

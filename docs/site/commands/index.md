---
title: Commands
description: Reference for every asn command, its flags, and arguments.
---

# Commands

> Placeholder page. Real content lands in a follow-up documentation task.

`asn` generates 249 invocable commands from the pinned Asana OpenAPI manifest, named
`asn <resource> <kebab-operation-id>`. Run `asn schema` to produce a full machine-readable
catalog of every command.

This page will eventually document each resource group (tasks, projects, workspaces,
attachments, webhooks, etc.) with examples and expected output.
